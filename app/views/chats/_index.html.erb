<% chats.each do |chat|%>
  <%if chat.user_id == current_user.id%>
    <div class="mycomment">
      <p><%= chat.message %></p>
    </div>
   <%else%>
    <div class="fukidasi">
      <div class="faceicon">
         <%= link_to user_path(chat.user_id), class:'user_name' do %>
           <%= attachment_image_tag chat.user, :profile_image, size: '30x30', format:'jpg', fallback:'no_image.png', class:'profile_image' %>
         <% end %>
       </div>
      <div class="chatting">
         <div class="says">
           <p><%= chat.message %></p>
         </div>
      </div>
    </div>
  <%end%>
<%end%>