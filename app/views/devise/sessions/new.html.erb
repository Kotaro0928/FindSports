<div class="devise_block">
  <h3>ログイン</h3>

  <%= form_with model: @user, url: user_session_path, local: true do |f| %>
    <div class="field">
      <%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "メールアドレス", size: "40", class: 'enter-field' %>
    </div>

    <div class="field">
      <%= f.password_field :password, autocomplete: "current-password", placeholder: "パスワード", size: "40", class: 'enter-field' %>
    </div>

    <div class="actions">
      <%= f.submit "ログイン", class: 'btn btn-primary sign-in-button' %>
    </div>
  <% end %>

  <%= render "devise/shared/links" %>
</div>